pipeline {
    agent any
    
    stages {
        stage('Clone git') {
            steps {
                git 'https://github.com/cmelgreen/PersonalSite'
            }
        }

        stage('Build') { 
            steps {
                sh 'npm run-script build'
            }
        }
    }
}